import 'package:flutter/material.dart';

class DocFinance extends StatelessWidget {
  const DocFinance({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Finance')),
      body: Center(child: Text('Finance')),
    );
  }
}
