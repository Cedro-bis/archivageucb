import 'package:flutter/material.dart';

class DocAcadem extends StatelessWidget {
  const DocAcadem({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Documents académiques')),
      body: Center(child: Text('Documents académiques')),
    );
  }
}
