import 'package:flutter/material.dart';

class Livres extends StatelessWidget {
  const Livres({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Livres')),
      body: Center(child: Text('Livres')),
    );
    ;
  }
}
