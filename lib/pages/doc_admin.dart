import 'package:flutter/material.dart';

class DocAdmin extends StatelessWidget {
  const DocAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Documents administratifs')),
      body: Center(child: Text('Documents administratifs')),
    );
    ;
  }
}
